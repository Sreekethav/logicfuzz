Improve fuzz driver coverage for project: {PROJECT_NAME}

CURRENT STATUS

Coverage Metrics:
- PC Coverage: {COVERAGE_PERCENT}
- Line Coverage Diff: {LINE_COVERAGE_DIFF}

Current Driver:
```
{CURRENT_CODE}
```

Coverage too low! Rewrite needed.

COVERAGE ANALYZER DIAGNOSIS

Root Cause:
{COVERAGE_INSIGHTS}

Recommended Actions:
{IMPROVEMENT_SUGGESTIONS}

TASK

Rewrite driver to maximize coverage by addressing the diagnosis above.

REQUIREMENTS:
1. Implement coverage analyzer's recommended actions
2. Use FuzzedDataProvider for all variable inputs
3. Replace fixed/constant parameters with varied inputs
4. Test edge cases (NULL, empty, boundary values)
5. Maintain correct API sequence order
6. Don't violate API lifecycle constraints

IMPROVEMENT STRATEGIES (priority order):

1. Vary more parameters:
```cpp
#include <fuzzer/FuzzedDataProvider.h>
extern "C" int LLVMFuzzerTestOneInput(const uint8_t *data, size_t size) {
  FuzzedDataProvider fdp(data, size);

  // Vary ALL inputs, not just the first one
  std::string input1 = fdp.ConsumeRandomLengthString(1024);
  std::string input2 = fdp.ConsumeRandomLengthString(512);
  int flags = fdp.ConsumeIntegral<int>();

  // Call API sequence with varied inputs
  ctx = api_create();
  api_configure(ctx, input1.data(), input1.size());
  api_process(ctx, input2.data(), input2.size(), flags);
  api_cleanup(ctx);
  return 0;
}
```

2. Test edge cases:
```cpp
// Test empty, short, and boundary inputs
if (size == 0) return 0;  // Handle empty input gracefully
// Test with minimal input
// Test with maximum reasonable input
```

3. Try different API sequences:
   - If current sequence has low coverage, try a DIFFERENT sequence
   - Focus on sequences that exercise different code paths

4. Vary struct fields:
```cpp
FuzzedDataProvider fdp(data, size);
struct Config cfg = {0};
cfg.field1 = fdp.ConsumeIntegral<int>();
cfg.field2 = fdp.ConsumeIntegralInRange<uint32_t>(0, 100);
api_init(ctx, &cfg);
```

OVERRIDE AUTHORITY:
- Change FIX -> varied when coverage low
- Try different API sequence if current one saturated
- Document rationale when overriding

COMMON ISSUES & FIXES:
- Fixed params -> Use FuzzedDataProvider
- Single sequence -> Try different sequences
- No edge cases -> Test NULL/empty/boundary
- Missing API calls -> Add more APIs from the same sequence

OUTPUT FORMAT:

<conclusion>Root cause, changes made, expected improvement</conclusion>
<fuzz_target>[Complete improved code]</fuzz_target>
<coverage_strategy>New paths targeted, why better coverage expected</coverage_strategy>

CRITICAL REQUIREMENTS:
- Follow exact XML format above
- Keep each tag brief (max 3 sentences)
- Include complete, compilable code in <fuzz_target> - NO placeholders
- NO markdown formatting around XML tags
- NO extra commentary outside XML tags
