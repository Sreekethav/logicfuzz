Generate a loop termination condition for fuzz driver.

Loop type: {loop_type}
API return type: {api_return_type}
Hint: {termination_hint}

Common patterns:
- iterator: "result != NULL"
- incremental: "bytes_read > 0"
- state_machine: "state != DONE"

Output ONLY the C condition expression.
