Analyze buffer-length parameter relationships in this C/C++ function.

Function: {signature}

Parameters:
{parameters}

For each buffer pointer, identify which parameter specifies its size and the relationship type.

Output JSON:
{{
    "relations": [
        {{"buffer_param": "data", "length_param": "len", "relationship": ">=", "reasoning": "..."}}
    ],
    "no_varlen": false
}}

Relationship types: "==" (exact), ">=" (minimum), "size*count", or custom expression.
Set no_varlen=true if no buffer-length relationships exist.
