Generate fuzz driver for project: {PROJECT_NAME}

API SEQUENCES (from static analysis):
The following API call sequences have been validated by lifecycle analysis.
Pick ONE sequence and implement a complete fuzz driver for it.

SRS SPECIFICATION:
{SRS_SPECIFICATION}

REFERENCE SKELETON (use these headers):
{SKELETON_CODE}

{ADDITIONAL_CONTEXT}

REQUIREMENTS:
1. Pick ONE API sequence from the provided list
2. Implement the COMPLETE sequence (all APIs in order)
3. Use skeleton headers only (filtered, safe)
4. Handle resource lifecycle: init -> use -> cleanup
5. Match buffer sizes with length parameters (var-len relationships)

PARAMETER STRATEGY:

DEFAULT: FuzzedDataProvider for splitting fuzzer input

```cpp
#include <fuzzer/FuzzedDataProvider.h>

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *data, size_t size) {
  FuzzedDataProvider fdp(data, size);

  // Consume data for each API call
  std::string input1 = fdp.ConsumeRandomLengthString(1024);
  int param1 = fdp.ConsumeIntegral<int>();

  // Call APIs in sequence order
  void* ctx = api_create(...);
  if (!ctx) return 0;

  api_init(ctx, input1.data(), input1.size());
  api_process(ctx, param1);

  api_cleanup(ctx);
  return 0;
}
```

VAR-LEN HANDLING:
- When buffer and size parameters are related, use the SAME source:
```cpp
std::string buf = fdp.ConsumeRandomLengthString(4096);
api_func(buf.data(), buf.size());  // size matches buffer
```

CALLBACK HANDLING:
- Use stub functions for callback parameters:
```cpp
void my_callback(void* ctx, int status) {
    // Minimal stub - just return
}
api_register_callback(ctx, my_callback, NULL);
```

IMPLEMENTATION STEPS:

1. Headers:
   - Use ONLY reference skeleton headers
   - Filter out internal/*, detail/*, private/*

2. Resource initialization:
   - Call create/init APIs first
   - Check return values before proceeding

3. API sequence execution:
   - Follow exact order from chosen sequence
   - Use FuzzedDataProvider for varied inputs
   - Handle var-len relationships

4. Cleanup:
   - Call cleanup/free/destroy APIs
   - Cleanup in REVERSE order of creation

OUTPUT FORMAT:

<conclusion>Chosen sequence, APIs called, var-len handling, cleanup order</conclusion>
<fuzz_target>[Complete LibFuzzer code with LLVMFuzzerTestOneInput]</fuzz_target>

CRITICAL REQUIREMENTS:
- Follow exact XML format above
- Keep <conclusion> brief (max 3 sentences)
- Include complete, compilable code in <fuzz_target> - NO placeholders
- NO markdown formatting around XML tags
- NO extra commentary outside XML tags
